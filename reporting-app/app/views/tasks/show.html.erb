<%= render template: 'strata/tasks/show', locals: {
  task: @task,
  assigned_user_display_text: @assignee.try(:email) || link_to("Assign to me", assign_task_path(@task), method: :post, class: "usa-button"),
  application_form: @application_form,
  kase: @case
} %>
