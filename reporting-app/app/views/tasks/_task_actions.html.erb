<% if task.pending? %>
  <%#
    # TODO: Replace with radio button design:
    # https://linear.app/nava-platform/issue/TSS-385
  %>
  <%= strata_form_with(model: task) do |f| %>
    <%= f.submit t("tasks.details.approve_button"), class: "usa-button--outline" %>
    <%= f.submit t("tasks.details.deny_button"), class: "usa-button--outline" %>
    <%= f.submit t("tasks.details.request_for_information_button"), class: "usa-button--outline" %>
  <% end %>
<% end %>
