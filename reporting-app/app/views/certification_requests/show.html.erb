<%= render partial: "application/breadcrumbs", locals: { crumbs: [
  { name: t("certification_requests.index.title"), url: certification_requests_path },
  ], current_name: @certification_request.id  } %>
<% content_for :title, t(".title") %>
<h1><%= t(".title") %></h1>
<%= render "form", certification_request_form: @certification_request, submit_text: t(".submit") %>
<h2><%= t(".activity_reports.header") %></h2>
<table class="usa-table usa-table--borderless width-full">
  <thead>
    <tr>
      <th scope="col">
        <%= t(".activity_reports.col_activity_report_id") %>
      </th>
      <th scope="col">
        <%= t(".activity_reports.col_cases") %>
      </th>
    </tr>
  </thead>
  <tbody>
    <% for activity_report in @certification_request.activity_report_application_forms do %>
      <tr>
        <td>
          <%= link_to activity_report.id, activity_report_application_form_path(activity_report) %>
        </td>
        <td>
          1
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
